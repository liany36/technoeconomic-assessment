{
  "name": "technoeconomic-assessment",
  "version": "1.0.0",
  "main": "./out/index.js",
  "author": "Yi Lian",
  "license": "MIT",
  "scripts": {
    "prebuild": "tslint -c tslint.json -p tsconfig.json",
    "build": "tsc",
    "dev": "yarn build && node .",
    "watch": "nodemon --watch . -e ts --exec yarn dev",
    "start": "npm run-script build && node ./out/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "body-parser": "^1.19.0",
    "dotenv": "^8.0.0",
    "express": "^4.17.1",
    "swagger-ui-express": "^4.0.7"
  },
  "devDependencies": {
    "@types/body-parser": "^1.17.0",
    "@types/dotenv": "^6.1.0",
    "@types/express": "^4.17.0",
    "@types/node": "^12.0.10",
    "@types/swagger-ui-express": "^3.0.1",
    "nodemon": "^1.18.9",
    "tslint": "^5.12.1",
    "tslint-eslint-rules": "^5.4.0",
    "typescript": "^3.5.3"
  }
}
